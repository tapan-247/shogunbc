import { ArgsTable, Meta, Preview, Story } from '@storybook/addon-docs'

import { shopifyCollection } from '../../lib/mocks'
import Banner from '.'

<Meta
  title="Sections/Banner"
  component={Banner}
  argTypes={{
    title: {
      control: 'text',
    },
    showTitle: {
      control: {
        type: 'boolean',
      },
    },
  }}
  parameters={{ layout: 'padded' }}
/>

# Banner

The Banner component is used to create a header 'h1' element with a backgroundImage.

## Props

<ArgsTable of={Banner} />

### Default

<Canvas>
  <Story
    name="Default"
    args={{
      image: shopifyCollection.image,
      title: 'Bikes',
      showTitle: true,
    }}
  >
    {args => <Banner {...args} />}
  </Story>
</Canvas>
