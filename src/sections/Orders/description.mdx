The `AccountOrders` section is responsible for showing the orders history or details of a single order. Internally it renders `AccountOrdersHistory` or `AccountOrderDetails` components.

A single order is shown when the `order-id` parameter is present in the URL.
