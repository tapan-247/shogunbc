import LinkTo from '@storybook/addon-links/react'

`ProductBox` accepts a `product` prop which is an object holding product data.
See a product example on the <LinkTo kind="docs-models--page#product">**Models page**</LinkTo>.
