import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import LinkTo from '@storybook/addon-links/react'
import { withQuery } from '@storybook/addon-queryparams'

import { getQueryDecorators } from '../../lib/decorators'

import Search from '.'

<Meta title="Sections/Search" component={Search} />

# Search

The `Search` section displays search results based on the search query in URL params: `...?q=search-text`.

## Props

No props available.

## Examples

### Default

<Canvas>
  <Story name="Default">
    <Search />
  </Story>
</Canvas>

### FromQueryParams

<Canvas>
  <Story
    name="FromQueryParams"
    parameters={{ query: { q: 'This input was from query params' } }}
    decorators={getQueryDecorators(
      { query: { q: encodeURIComponent('This input was from query params') } },
      '/search',
    )}
  >
    <Search />
  </Story>
</Canvas>
